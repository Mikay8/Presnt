version: 1
applications:
  - appRoot: artifacts/presnt
    frontend:
      phases:
        preBuild:
          commands:
            - npm install -g pnpm@10
            - cd /codebuild/output/src*/src/Presnt && pnpm install --no-frozen-lockfile
        build:
          commands:
            - cd artifacts/presnt && node_modules/.bin/expo export -p web
      artifacts:
        baseDirectory: dist
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
          - ../../node_modules/**/*
  - appRoot: artifacts/api-server
    frontend:
      phases:
        preBuild:
          commands:
            - npm install -g pnpm@10
            - cd /codebuild/output/src*/src/Presnt && pnpm install --no-frozen-lockfile
        build:
          commands:
            - cd artifacts/api-server && pnpm run build
      artifacts:
        baseDirectory: dist
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
          - ../../node_modules/**/*
