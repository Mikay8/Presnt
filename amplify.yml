version: 1
applications:
  - appRoot: artifacts/mockup-sandbox
    frontend:
      phases:
        preBuild:
          commands:
            - npm install -g pnpm@10
            - cd /codebuild/output/src*/src/Presnt && pnpm install --no-frozen-lockfile
        build:
          commands:
            - pnpm run build
      artifacts:
        baseDirectory: dist
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
          - ../../node_modules/**/*
