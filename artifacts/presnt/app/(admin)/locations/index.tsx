/**
 * Admin — Saved Locations
 * Re-exports the officer locations screen (same component, same permissions).
 * Admin has implicit manage_events permission so all actions are available.
 */
export { default } from '@/app/(officer)/locations/index';
